import { Button } from '../shared/components/Button';

const RetweetButton = () => {
  return (
    <Button startIcon="autorenew" size="sm">
      9
    </Button>
  );
};

export default RetweetButton;
